# AGENTS.md

このリポジトリは、Go で SQLite の内部構造を学びながら、最小構成のデータベースエンジンを実装するための学習プロジェクトです。

## 基本方針

- シンプルで理解しやすい実装を優先する
- SQLite の本質に集中し、過剰な抽象化や不要な依存を避ける
- 1 つの概念ずつ小さく実装し、テストで検証する
- 実装の説明や設計意図は README とコメントで明確にする

## 開発ルール

### 1. Go らしい実装を心がける

- 標準ライブラリを優先する
- パッケージ構成は責務ごとに分離する
- 関数は小さく、1 つの役割に集中させる
- 変数名と関数名は意味が伝わる名前にする

### 2. テストを重視する

- 新しい機能や修正には最低 1 件のテストを追加する
- バグ修正時は再現ケースを先に作成してから修正する
- `go test ./...` を基本の確認コマンドとする

### 3. 変更範囲を最小化する

- 必要最小限のファイルだけを編集する
- 仕様変更や設計変更は、まず README と構成を整理してから実装する
- 実装の意図が曖昧な場合は、関連するドキュメントを先に整える

### 4. TODO と設計の追跡

- 実装の進行中に明確な未完了点がある場合は、README やコードコメントに簡潔に残す
- 大きな機能追加の前に、目標とスコープを明示する

## 推奨したリポジトリ構成

```text
.
├── README.md
├── AGENTS.md
├── .gitignore
├── go.mod
├── cmd/
│   └── app/
├── internal/
│   ├── db/
│   ├── parser/
│   ├── storage/
│   └── sql/
├── pkg/
│   └── sqlite/
├── testdata/
└── ...
```

## よく使うコマンド

```bash
go test ./...
```

```bash
go run ./...
```

```bash
go run ./cmd/app
```

## 実装の優先順位

1. 低レベルのストレージとページ管理
2. レコードの読み書きとエンコーディング
3. SQL の最小構文解析
4. テーブル/インデックスの概念整理
5. INSERT / SELECT / UPDATE の基本動作
6. トランザクションや耐久性の検討

## ドキュメント方針

- README は利用者が最初に読む導入資料として整理する
- 実装の設計意図や制約は README に簡潔に残す
- 仕様の変化に合わせて README を更新する
- AGENTS.md は開発ルールと作業方針の中心として維持する

## 禁止事項

- 不要な依存の追加
- 設計が曖昧な状態で大規模な実装に着手すること
- テストなしで機能を増やすこと
- 本来の学習目的から離れる過剰な実装

## 期待する成果物

- 追加・修正ごとに動く Go コード
- その挙動を確認できるテスト
- 読み手が理解しやすい README と設計の説明

このリポジトリでは「SQLite を再現する」ことよりも、内部構造を理解しながら学ぶことを重視します。